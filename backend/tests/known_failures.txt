# Known-failing tests — QUARANTINE, NOT FORGIVENESS
#
# This list is currently EMPTY. Every test runs and is expected to pass.
#
# When a test genuinely can't be fixed immediately, add its node id here (one per
# line) with a comment explaining the root cause, so the suite can stay a real
# blocking gate instead of going red and being ignored. A CI step re-runs
# everything listed here and FAILS if any of them passes, so a fixed test can't
# rot in quarantine.
#
# ---------------------------------------------------------------------------
# RESOLVED — meetings / LiveKit egress (was 14 tests)
# ---------------------------------------------------------------------------
# These failed only LOCALLY: the livekit-api SDK (a declared dependency,
# pyproject.toml) wasn't installed in the local venv, so start_meeting's
# `from livekit.api import LiveKitAPI` raised. CI installs `.[dev]` and always
# had the SDK, so 13 of the 14 actually passed there all along.
#
# The 14th (test_meeting_templates::test_internal_sync_keeps_recording_off) was a
# real bug: Commit 25 changed MeetingCreate.record_meeting to default True, which
# silently defeated Commit 16's template default ("only override when falsy" can
# never fire when the default is True), so INTERNAL_SYNC recorded anyway. Fixed by
# making record_meeting Optional (None = "caller omitted") so the template default
# can decide, while an explicit value still wins.
