// Bump when the runtime's behaviour or CSS changes in a way published
// pages must pick up. Mirrors PAGES_RUNTIME_VERSION in
// backend/app/pages/compiler.py — the compiler links
// /api/pages/public/runtime/<version>/runtime.{js,css}.
export const RUNTIME_VERSION = '2026.09.1'
